// Native
// São definidos na especificação da linguagem e são implementados independente do host (navegador).

// Host
// Objetos do host são implementados pelo próprio ambiente (browser). Por exemplo no browser possuímos objetos do DOM, como DomList, HTMLCollection e outros. Em Node.js os objetos do Host são diferentes, já que não estamos em um ambiente do browser.

// User
// São objetos definidos pelo seu aplicativo. Ou seja, qualquer objeto que você criar ou importar de alguma biblioteca

// Eercícios
// Liste 5 objetos nativos
// String, Number, Boolean, Array e Function

// Liste 5 objetos do browser
// HTMLCollection, NodeList, window, console e Document

// Liste 2 Métodos, Propriedades ou Objetos
// presentes no Chrome que não existem no Firefox
